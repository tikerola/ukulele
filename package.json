{
  "name": "ukesync",
  "private": true,
  "scripts": {
    "dev": "concurrently --names frontend,backend --prefix-colors cyan,yellow --kill-others-on-fail \"npm run dev --prefix frontend\" \"cd backend && python -m uvicorn main:app --reload --port 8000\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
